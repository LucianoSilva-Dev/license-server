# Porta do servidor (default: 3001)
PORT=3001

# Secret para autenticar rotas admin (/admin/*)
# Se não definido, será gerado automaticamente no primeiro start
ADMIN_SECRET=

# Arquivo do banco SQLite (default: data/licenses.sqlite)
DB_PATH=data/licenses.sqlite
